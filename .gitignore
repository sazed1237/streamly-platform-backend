# Node modules (packages)
node_modules/

#uploads directory
# This is where uploaded files are stored, and it should not be committed to the repository.
uploads/
# Environment variables (never commit secrets)
.env

# Prisma generated files (client and other cache)
prisma/generated/
.prisma/

# OS system files
.DS_Store
Thumbs.db
/temp_uploads/
.temp_uploads
/tmp_uploads
# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

minio-data/.minio.sys/buckets/
minio-data/.minio.sys/pool.bin
minio-data/.minio.sys/config/
minio-data/.minio.sys/multipart/
minio-data/.minio.sys/tmp/
minio-data/.minio.sys/format.json
minio-data/temp
minio-data/test
minio-data/videos-dev


# Optional: editor/IDE settings
.vscode/
.idea/

# Optional: OS junk
*.swp

# Optional: coverage reports
coverage/

# Build outputs (if using TypeScript or Babel)
dist/
build/

# Optional: database files if using SQLite
*.sqlite
*.db

# Optional: dotenv files for different envs
.env.*
